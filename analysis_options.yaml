include: package:lints/recommended.yaml

analyzer:
  errors:
    avoid_print: ignore
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**

linter:
  rules:
    non_constant_identifier_names: false